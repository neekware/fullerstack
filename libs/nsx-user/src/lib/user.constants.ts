/**
 * User profile per page - pagination
 */
export const USER_PER_PAGE = 25;

/**
 * JWT session expiry in days
 */
export const USER_JWT_EXPIRY = 14;


/**
 * Salt hash creation cost factor
 * The cost factor controls how much time is needed to calculate a single BCrypt hash
 */
export const USER_PASSWORD_SALT_ROUNT = 10;
import { TmpLetDirective } from './tmp-let.directive';

describe('TmpLetDirective', () => {
  it('should create an instance', () => {
    const directive = new TmpLetDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
